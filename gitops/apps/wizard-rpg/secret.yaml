apiVersion: v1
kind: Secret
metadata:
  name: wizard-rpg-secret
  namespace: wizard-rpg
  labels:
    app.kubernetes.io/name: wizard-rpg
    app.kubernetes.io/part-of: wizard-rpg
type: Opaque
stringData:
  # IMPORTANT: Replace these placeholder values before applying.
  # In production, use SealedSecrets or an external secret manager.
  # The DB credentials are managed by CloudNativePG (see database/cluster.yaml).
  # Reference the CNPG-generated secret 'wizard-rpg-db-app' for DB credentials.
  APP_SECRET_KEY: "CHANGE_ME_BEFORE_DEPLOY"
